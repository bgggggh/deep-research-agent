# deep_research_agent requirements
# 全部免费，无需付费 API

# LangGraph + LangChain 核心
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# LLM 客户端（按需装）
langchain-google-genai>=2.0.0   # Gemini 免费 tier
langchain-groq>=0.2.0            # Groq 免费 tier
langchain-ollama>=0.2.0          # 本地 Ollama

# 搜索（免费，无需 API key）
ddgs>=6.0.0

# 网页抓取
httpx>=0.27.0
beautifulsoup4>=4.12.0

# 向量存储（复用你现有项目的 ChromaDB）
chromadb>=0.5.0

# Embedding（复用你现有的 BGE-M3）
sentence-transformers>=3.0.0

# 工具
python-dotenv>=1.0.0
